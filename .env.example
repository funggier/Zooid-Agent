# Documentation example only. Zooid does not auto-load this file.
# Copy values into your shell environment; never commit a real API key.

ZOOID_PROVIDER=openai-compatible
ZOOID_PROVIDER_BASE_URL=http://127.0.0.1:11434/v1
ZOOID_PROVIDER_MODEL=gpt-oss:20b
# ZOOID_PROVIDER_API_KEY=
ZOOID_PROVIDER_TIMEOUT_MS=120000
